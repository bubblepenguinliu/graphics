                     第 5 章  拉普拉斯平滑 

   有时，我们希望消去网格的棱角来得到更柔滑的形状，这就好像是从完成的泥塑向着一团泥坯的过程倒退， 
通常叫做平滑1 。本实验中，你将实现拉普拉斯平滑算法，这是一种经典且比较容易实现的平滑方法。 

 5.1 实验内容 

   理解拉普拉斯算子和拉普拉斯平滑，实现 cotangent 权重形式的拉普拉斯平滑算法。这是一个挑战任务，你 

需要根据给出的原理思路自己推导一部分计算过程，并实现为代码。 

 5.2 指导和要求 

 5.2.1 拉普拉斯算子与平滑 

   平滑操作的前提是如何定义“平滑”。我们一起来回忆一些微分知识： 

   连续曲面上任意一点 x 处的曲率与它的二阶导数有关 

   沿不同的剖切方向，同一点的曲率有可能是不同的 

   那么，如何用一个指标度量点 x 处的平滑程度呢？从直观角度来看，各方向的曲率都会影响平滑程度，如 

果每个方向上曲率都很小，那这一点处肯定很平滑。但反过来呢？如果有些方向上曲率很小，另一些方向曲率 

较大，我们应该对无穷多个曲率值取平均？ 

   这需要通过 Laplacian-Beltrami 算子 ∆S  实现。∆Sx 是一个向量，它的模与平均曲率有关，方向为法线方向。 

我们希望 ∥∆Sx ∥尽可能小，从而让平均曲率尽可能小。 

   这个算子的定义可以被推广到离散的三角形网格上，Laplacian-Beltrami 算子作用于顶点 v 的结果是：（H 为 
                                              5 
平均曲率）                                        2 
                                            0 
                                X 
                                          2 
                     ∆S (v) = −2H n = w vi i  −v 
                               vi ∈N1 (v) 组 

   而每个邻接顶点 vi 的权重取决于 1-环邻域（参见定义 3.1）上左右两个角的余切值： 
                                    题 
                            1    课 
                       wi = k · (cot αi  + cot β )i 
                       α = ∠ 2 学 

                        i  v  vi i+1v 

                       β = ∠ 形 
                        i  v  vi i−1v 

                          图1 
                          P 
                        k = 
                            i wi 
                       机 
   从上面的式子里可以看到，∆S v 是“邻域的某种均值”与“自身”之差，那么我们将 ∆S v 加到 v 上，“自身”的 
                    算 
成分就会被削弱，得到的结果会趋于“邻域的均值”——这就是平滑。于是我们得到了拉普拉斯平滑的迭代形式： 

                  计 
               U          v ←v + λ∆S v 
              T 
1英文称 smoothing ，但光顺 (fairing) 有时亦称 smoothing ，注意这两个问题含义不同 
             J 
           X 
﻿                                       5.3  提交和验收 

5.2.2 要求 

  你需要根据上述材料，自己推导并考虑清楚拉普拉斯平滑的实现细节，并为 HalfedgeMesh 类新增一个 

smooth  方法，实现拉普拉斯平滑。另外，你还需要在 Toolbar ::model_mode 中增加一个按钮，以便从 UI 

界面上调用该算法。每点击一次按钮，整个 mesh 迭代一轮。 

  实现算法后，你需要加载 bunny.obj 执行平滑操作。有兴趣的话，也可以尝试平滑 dragon2.dae 这种更大规 

模的模型。它们都来自斯坦福大学公开的扫描数据集，带有一些来自扫描重建的噪声，比较适合用于测试平滑 

算法。 

5.3 提交和验收 

  你需要提交 bunny.obj 迭代 1 次、3 次、5 次和 10 次的结果截图。λ 可以任取，但取值应当使得四个结果有 

所差异，不应该太大（四个结果都过度平滑）或太小（四个结果都没什么变化）。其他按默认要求提交即可。 

  验收时需要现场演示拉普拉斯平滑并回答相关问题。如果你能够将网格变得更加平滑，可以得 5 分；如果 

你所实现的确实是 cotangent 权重的拉普拉斯平滑且能够正确地处理 bunny_hole.dae 这个带有边界的模型（平滑 

之后边界不会大幅度扩大或收缩），可以再得 5 分，共计 10 分。 

                                 25 
                               20 
                             组 
                           题 
                         课 
                       学 
                     形 
                   图 
                 机 
               算 
             计 
          TU 
         J 
        X 

                      24 
﻿                    第 6 章  隐式曲面与 Mesh 

   在很多情况下，想要得到曲面的参数方程（显式曲面）相对困难，而将其表达为一个标量场的等值面（隐式 

曲面）则容易得多。例如图6.1中由两个形体求交或求并得到的形状，就很难给出显式表示。由于我们无法直接观 

察隐式曲面，将其提取为 Mesh 常常是渲染或几何处理中的重要一环。本节中，你将实现经典的 Marching Cubes 

算法（有时译作“行进立方体”算法），从而完成符号距离场 (Signed Distance Field, SDF) 与 Mesh 的相互转换。 

                    图 6.1: 一些用 SDF 表示的三维形体 [4] 

6.1 实验内容 

   理解符号距离场的基本概念和 Marching Cubes 算法的原理，实现根据 Mesh 计算 SDF 和从 SDF 恢复 Mesh 

的过程。这是一个挑战任务，你需要自己查找部分资料并设计实现细节。 

6.2 指导和要求 

6.2.1 符号距离场                                   25 
   符号距离场是有“内外”之分的距离场。对任何一个封闭曲面 S 而言，其对应的符号距离场是一个函数 SDF :0 
                                           2 
R3  →R ， 

   当点 p 在曲面 S  内部时，SDF(p) 的值为 −d(p, S)  组 
   当点 p 在曲面 S 外部时，SDF(p) 的值为 d(p, S) 题 

   式6.1给出了一个简单的例子，这个 SDF 的零等值面就是单位球面。 
                                  课 

                                2      3 
                                学 
                       SDFsphere (x) = x −1,  x ∈R          (6.1) 

   更复杂的 SDF 本身可能也难以写出解析形式，不过初中数学课本初识函数时就提到，我们可以用列表的方形 
法表示函数。对于 SDF 而言，就是用空间中一系列采样点上的取值来有损地近似记录场函数的信息。为了方便图 

计算，我们通常将一个立方体范围内的空间划分成等大的小立方体格子，然后记录格子顶点上的 SDF 值。如果 
                       机                       3 
每个方向等分成 N 份，最终就会得到一个 N  ×N  ×N  的三维表（数组），我们将 N  称为这个 SDF 的分辨率 。 
以下讨论 SDF 时，默认讨论的都是这样近似表示的 SDF 。算 
                  计 
6.2.2 Marching Cubes 算法U 
              T 
   因为 SDF(x) 的 k-等值面就是 SDF(x) −k 的零等值面，所以提取任意等值面的问题可以很容易地转换为提J 
取零等值面的问题。而零等值面是 SDF 正值与负值的分界，所以每当我们找到取值一正一负的两个点，我们便X 

可以断定这亮点的连线与零等值面相交（虽然不能确定相交了多少次）。Marching Cubes 算法正是基于这一思想 
﻿                                           6.2  指导和要求 

设计的。图6.2是用 Marching Cubes 从 SDF 中提取一只小猫的例子，红点处 SDF 取值为正、绿点处 SDF 取值为 

负（虽然图中颜色有一些不准确的地方，但大致趋势正确）。 

              图 6.2: Marching Cubes 算法提取 Mesh 的例子 [5] 

  Marching Cubes 算法的特点是每个小立方体格子相互独立，算法只考虑单个格子顶点取值正负的各种情况。 

比如说，图6.3中红点为负，那么我们认为红点（内部）和其他顶点（外部）之间存在且只存在一个小的平面片， 

它是零等值面的一部分。通过提取每个格子中的小等值面片，就可以拼合出完整的零等值面。 

                                      5 
                 图 6.3: 几种简单的顶点正负模式 [1] 2 
                             8     20 
  一个立方体有 8 个顶点，那么不同的正负取值模式总共有 2 = 256 种。不过，图6.3中的三种模式可以通过 

旋转相互转化，而图6.4中的两种模式则互为“镜像”。借助旋转和镜像两种策略，实现 Marching Cubes 算法并不 
                                组 
需要硬编码 256 个模式。                题 
                            课 
                          学 
                       形 
                     图 
                   机 
                    图 6.4: 另外两种模式 [1] 
                 算 
               计 
6.2.3 要求     U 
           T 
  你需要根据上述材料，自己搜索资料、学习并推导实现 Marching Cubes 算法所需要的所有正负取值模式。为J 
了在图形界面上展示你的结果，你需要完成两项工作：X 

                         26 
﻿                                           6.3  提交和验收 

   首先实现根据被选中物体的 Mesh 计算 SDF 格子取值的方法，并将计算结果保存到文件中，你可以参考 

   ui/menubar.cpp 中的代码在菜单栏上新增一个 Export SDF To File 的选项，并通过读取 
   Scene ::selected_object  来获取当前被选中的物体。为了实现简单，分辨率可以固定为 1283 ，计算 

   SDF 的方法效率不作要求。 

   然后实现加载 SDF 文件并执行 Marching Cubes 算法提取 Mesh 的方法，并将提取到的 Mesh 添加到场景中。 

   你可以参考 ui/menubar.cpp 中 Import File as a Group 选项的实现来获取文件路径，并参考 Scene ::load 

   和 Group ::load 方法来创建新的组、物体和 Mesh 。 

  上面的功能放在哪个文件、哪个类中由你自己决定，SDF 数据的存储格式也由你自己决定。实现算法后，你 

应该能将被选中物体导出成 SDF 数据文件，再加载该文件并执行 Marching Cubes 算法重新得到 Mesh ，且提取 

的 Mesh 与原始 Mesh 应该相当接近。由于 SDF 的等值面一定是封闭流形，实现 Marching Cubes 算法的过程中 

你无需考虑任何不良的几何结构，并且只需要用 sphere.obj 、bunny.obj 和 cow.dae 这三个封闭的模型来测试即可 

满足要求。 

6.3 提交和验收 

  你需要提交根据 bunny.obj 计算 SDF 再提取 Mesh 后得到的结果截图，其他按默认要求提交即可。 

  验收时需要现场演示导出 SDF 数据文件和提取 Mesh 的过程，并介绍自己设计的 SDF 数据文件格式。如果 

你能够正确地计算 SDF 值并保存到文件中，可以得 2 分；如果你提取出的 Mesh 形状无误，可以再得 5 分；如 

果 Mesh 的法线正确（一致向外，渲染时没有明显的前向面、后向面混合问题），可以再得 3 分，共计 10 分。 

                                     25 
                                   20 
                                组 
                              题 
                            课 
                          学 
                       形 
                     图 
                   机 
                 算 
               计 
           TU 
           J 
         X 

                         27 
